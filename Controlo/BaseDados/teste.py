from Controlo.BaseDados.database import database
import time

if __name__ == '__main__':
    db = database()
    # db.new_connection(1,0)
    # db.update_green_power(1, 1)
    # db.update_green_power(1,0)
    # db.start_charging(1, 10, 1, 0, 0, 1)
    # db.new_measure(1, 1, 20, 5)
    # db.stop_charging(1, True, 0)
    # db.start_charging(1, 10, 1, 0, 0, 1)
    # db.new_measure(1, 1, 20, 5)
    # time.sleep(3)
    # db.new_measure(1, 1, 10, 10)
    # time.sleep(3)
    # db.stop_charging(1, True, 0)